export const MODEL_HEIGHT = 480;
export const MODEL_WIDTH = 480;
export const VIDEO_HEIGHT = 480;
export const VIDEO_WIDTH = 640;
export const X_RATIO = Math.max(VIDEO_WIDTH, VIDEO_HEIGHT) / VIDEO_WIDTH;
export const Y_RATIO = Math.max(VIDEO_WIDTH, VIDEO_HEIGHT) / VIDEO_HEIGHT;